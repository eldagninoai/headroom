---
import Layout from '../layouts/Layout.astro';

const aboutImage = "https://images.unsplash.com/photo-1598653222000-6b7b7a552625?q=80&w=2070&auto=format&fit=crop";
---

<Layout title="Nosotros | Headroom">
  
  <main class="w-full bg-black text-white about-padding">
    
    <div class="about-container mb-[10vh]">
      <h1 class="font-jost font-black uppercase leading-[0.9] text-white fluid-h1">
        No hacemos ruido.<br>
        <span class="text-gray-700">Hacemos historia.</span>
      </h1>
    </div>

    <div class="about-container flex flex-col min-[1025px]:flex-row gap-[8vw] min-[1025px]:gap-[5vw]">
      
      <div class="w-full min-[1025px]:w-1/2">
        <h3 class="text-[#F1EE14] font-mono uppercase tracking-[0.2em] mb-6 fluid-subtitle">El Manifiesto</h3>
        <p class="font-mono text-gray-400 leading-relaxed fluid-p mb-6">
          En un mundo saturado de imágenes, el audio es la última frontera de la atención. Headroom nació con una obsesión: <strong class="text-white">elevar el estándar auditivo</strong> de las marcas que se atreven a sonar diferente.
        </p>
        <p class="font-mono text-gray-400 leading-relaxed fluid-p">
          Somos ingenieros, músicos y estrategas. No solo ajustamos niveles; diseñamos emociones. Desde la mezcla más sutil hasta el estruendo de un festival, nuestra firma es la precisión.
        </p>
      </div>

      <div class="w-full min-[1025px]:w-1/2">
        <div class="image-wrapper w-full overflow-hidden rounded-xl grayscale hover:grayscale-0 transition-all duration-700">
          <img src={aboutImage} alt="Estudio Headroom" class="w-full h-full object-cover" />
        </div>
      </div>

    </div>

  </main>
</Layout>

<style>
  /* --- 1. MÓVIL / TABLET (Base) --- */
  .about-padding {
    padding-top: 15vh;
    padding-bottom: 10vh;
  }
  .about-container {
    padding-left: 6vw;
    padding-right: 6vw;
  }

  /* Tipografía Móvil */
  .fluid-h1 { font-size: 13vw; }
  .fluid-subtitle { font-size: 3vw; }
  .fluid-p { font-size: 3.5vw; }

  .image-wrapper {
    height: 60vw;
  }

  /* --- 2. ESCRITORIO (1025px+) --- */
  /* VW PURO para escalado infinito */
  @media (min-width: 1025px) {
    .about-padding {
      padding-top: 25vh; /* Ajuste para el Navbar Fixed */
      padding-bottom: 15vh;
    }
    .about-container {
      padding-left: 4.2vw;
      padding-right: 4.2vw;
    }

    /* Tipografía Escritorio (VW PURO) */
    .fluid-h1 { font-size: 7vw; }
    .fluid-subtitle { font-size: 0.8vw; }
    .fluid-p { font-size: 1.1vw; }

    .image-wrapper {
      height: 35vw;
    }
  }
</style>